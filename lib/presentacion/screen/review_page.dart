 import 'package:flutter/material.dart';

class ReviewPages extends StatelessWidget {
   const ReviewPages({super.key});
 
   @override
   Widget build(BuildContext context) {
     return const Scaffold(
      body:Center(child:Text('Repasar Pages'))
     );
   }
 }